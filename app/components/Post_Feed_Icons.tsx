import React from 'react'
import { ManueButton} from '../Types/ManueButton'

export const Post_Feed_Icons:React.FC<ManueButton> = ({title,link,icon}) => {
  return (
    <div>Post_Feed_Icons</div>
  )
}
